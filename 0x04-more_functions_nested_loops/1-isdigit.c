#include "main.h"
/**
 * _isdigit - A function that checks if an entry is a number
 * @c: takes a formal integer parameter called c
 * Return: 1 if digit and 0 if otherwise
 */
int _isdigit(int c)
{
	if (x >= 48 && x <= 57)
	{
		return (1);
	}
	return (0);
}
