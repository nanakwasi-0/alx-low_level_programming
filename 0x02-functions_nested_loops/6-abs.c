#include "main.h"
/**
 * _abs - This function computes the absolute value of an int
 * @n: The value to be converted.
 * Return: 0(Success!!)
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (n * -1);
	}
}

